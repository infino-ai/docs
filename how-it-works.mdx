---
title: How Infino works
icon: "diagram-project"
description: How Infino works. One copy of your data stored as Apache Parquet, indexed and queryable four ways (SQL, BM25, vector, hybrid), all in-process.
---

Infino keeps your data as Apache Parquet and builds its search indexes right into
those files. You can search it with full-text, vector, SQL, or a hybrid of all three,
and it runs inside your application.

```text
       write rows   ──►  ┌─────────────── supertable ───────────────┐
  (text · vectors ·      │  manifest: snapshot reads, atomic commits │
       scalars)          │                                           │
                         │    superfile ── superfile ── superfile    │
                         │      each = one Apache Parquet file with   │
                         │      BM25 + vector (IVF + RaBitQ) indexes  │
                         └──────────────────┬────────────────────────┘
                                            │  on S3 / Azure / local disk
                                            ▼
              query the same rows from your own process:
      full-text (BM25) · vector kNN · SQL · hybrid (BM25 + vector, fused with RRF)
```

## The two layers

- **Superfile**: a single Apache Parquet file with embedded BM25 and vector
  (IVF + RaBitQ) indexes spliced in. It is immutable once written, and still a valid Parquet
  file, so anything that reads Parquet can read your data.
- **Supertable**: composes many superfiles into one queryable table, with a manifest
  that provides snapshot-isolated reads and atomic commits. Writes are append-only,
  and `update` / `delete` are handled without rewriting your data.

## One copy, four query modes

Index your rows once, then retrieve them however the question needs:

- **Full-text (BM25)**: keyword search.
- **Vector (kNN)**: semantic search over your embeddings (bring your own).
- **SQL**: filter, aggregate, and join over the same rows.
- **Hybrid**: BM25 and vector fused in a single query with reciprocal-rank fusion (RRF).

Infino runs **inside your application** and works from one copy of your data, so the
same Parquet files serve every kind of search.

## Dig deeper

<CardGroup cols={2}>
  <Card title="Object-storage-native retrieval" icon="cloud" href="/concepts/object-storage-native-retrieval">
    The core idea: search that runs directly on Parquet in object storage.
  </Card>
  <Card title="Guides" icon="book" href="/guides/tables">
    How-tos for tables, embeddings, indexing, search, and storage.
  </Card>
</CardGroup>

For the exhaustive, code-level internals, covering the superfile format, the supertable
manifest, and the query layers, see [Infino on DeepWiki](https://deepwiki.com/infino-ai/infino).
